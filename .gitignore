# caches / OS
__pycache__/
*.pyc
.DS_Store
.pytest_cache/

# large datasets + model weights — never committed (see DATA.md to obtain)
data/
*.pth
*.pt.tar
*.mat
*.h5

# archived / non-essential material, kept locally but out of the repo
attic/

# FPGA build outputs (regenerated by `make` in hw/)
hw/*.json
hw/*.asc
hw/*.bin
hw/*.rpt
hw/*.log
hw/tb.vvp

# paper (LaTeX source + build) — not tracked
paper/

# figures: commit ONLY the paper figures + the cached 16-recording eval;
# everything else in figures/ is regenerated by scripts/
figures/*
!figures/before_after.png
!figures/downstream_real.png
!figures/edncnn_efficiency.png
!figures/event_picture.png
!figures/pareto.png
!figures/stdp_learning.png
!figures/edncnn_real.json
!figures/data/
figures/data/*
!figures/data/edncnn_real.json
!figures/data/esr.json
